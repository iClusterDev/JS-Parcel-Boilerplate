const Header = () => {
  const template = `
    <header>
      <h1>My Parcel App</h1>
      <p>boilerplate for a simple JS workflow</p>
    </header>
  `;

  return template;
};

export default Header;
